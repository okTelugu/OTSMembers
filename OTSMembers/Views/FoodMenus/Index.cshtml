@model IEnumerable<OTSMembers.Models.FoodMenu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Occassion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NotesIfAny)
        </th>
        <th>
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Occassion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NotesIfAny)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Menu Details>>", "Details","FoodMenus", new { id = item.Id }, htmlAttributes: new { @class = "btn-primary" })|
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
