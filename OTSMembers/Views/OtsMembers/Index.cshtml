@model IEnumerable<OTSMembers.Models.OtsMember>

@{
    ViewBag.Title = "Index";
}

<h2>Oklahoma Telugu Sangham</h2>
<h3>Membership Home Page</h3>

@*<form class="emailForm input-group-lg" role="search" method="post">
    <div class="col-md-6">
        <div class="form-group">
            <br />
            <label>Search Directory by Email</label>
            <input type="email" class="form-control" placeholder="Enter Email" name="searchEmail">
            <button type="submit" class="btn-success">Search</button>
        </div>
    </div>
</form>*@
@*<form class="nameForm input-group-lg" role="search" method="post">
    <div class="col-md-6">
        <div class="form-group">
            <label>Search Directory by Name</label>
            <input type="text" class="form-control" placeholder="First Name" name="firstName">
            <input type="text" class="form-control" placeholder="Last Name" name="lastName">
            <button type="submit" class="btn-success">Search</button>
        </div>
    </div>
</form>*@
@if (Model.Count() == 0)
    {
        <label class="alert-info">"Membership information is not found with the logged in email! If you want to login with a different email, please log out and login with the different email. Alternatively, you can register as a new member by clicking the link below."</label>
        <h4>@Html.ActionLink("Register as a New Member", "Create") </h4>
    }
<table class="table table-hover">

    <tr>
        @if (Model.Count() > 0)
        {
            <th>
                @Html.DisplayNameFor(model => model.FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            }
    </tr>
@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.ActionLink("Member & Sponsorship Details>>", "Details", new { id = item.id }, htmlAttributes: new {@class = "btn btn-primary" }) 
                        @*@Html.ActionLink("Delete", "Delete", new { id = item.id })*@
        </td>
    </tr>
}

</table>
<div class="col-md-offset-3">
    <div class="embed-responsive embed-responsive-4by3">
        <iframe class="embed-responsive-item" src="//www.youtube.com/embed/RDPJbgkfATA"></iframe>
    </div>
</div>